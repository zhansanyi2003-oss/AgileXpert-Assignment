version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: agileexpert-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: agileexpert
      POSTGRES_USER: user
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
